import './app.css';

import ErrorIndicator from '../error-indicator';
import Spinner from '../spinner';

const App = () => {
    return (
        <div className="app">
            <Spinner />
        </div>
    )
};

export default App;